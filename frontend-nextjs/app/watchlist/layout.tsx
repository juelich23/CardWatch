import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Watchlist | CardWatch',
};

export default function WatchlistLayout({ children }: { children: React.ReactNode }) {
  return children;
}
